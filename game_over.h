#ifndef GAME_GAME_OVER_H
#define GAME_GAME_OVER_H

#include "game_state.h"


/**
 * Update function for the game over part.
 */
void update_game_over(input_controller *c, game_state* game);

/**
 * Render function for the game over part.
 */
void render_game_over(game_state* game);


#endif

